/** Canonical design system identifier - single source of truth for Salt / M3 / Fluent / uoaui / Carbon */
export type DesignSystemId = 'salt' | 'm3' | 'fluent' | 'uoaui' | 'carbon';

/** Theme mode across all design systems */
export type ThemeMode = 'light' | 'dark';
