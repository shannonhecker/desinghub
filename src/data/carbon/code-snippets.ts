/* Carbon code snippets - Phase 1 stub.
   Keyed by componentId (matches entries in CARBON_COMPONENTS).
   Phase 2 ships ~80 entries using @carbon/react imports and
   semantic Carbon SCSS tokens. */

import type { CodeSnippets } from "../salt/types";

export const CARBON_CODE: CodeSnippets = {};
