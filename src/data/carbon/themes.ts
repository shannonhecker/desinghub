/* Carbon theme re-export + typed consumer API.
   The canonical dictionaries live in carbon-documentation.jsx so the
   module stays a single source of truth. Phase 2 extends both the
   token set inside CARBON_THEMES and surfaces additional helpers
   (per-theme spacing scale, per-theme elevation, etc). */

export { CARBON_THEMES } from "./carbon-documentation.jsx";
