/* Carbon component registry - Phase 1 stub.
   Phase 2 replaces this with ~80 entries mirroring Carbon's full
   @carbon/react component list (buttons, inputs, data display,
   feedback, navigation, layout, foundations, patterns). */

import type { ComponentDef } from "../salt/components";

export const CARBON_COMPONENTS: ComponentDef[] = [];
